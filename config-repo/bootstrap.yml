server:
  port: 8888

spring:
  application:
    name: cloud-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/karitoo/config-server/config-repo
          username: karitoo
          password: 147258
          timeout: 5
          skip-ssl-validation: true